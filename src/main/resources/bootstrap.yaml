spring:
  config:
    activate:
      on-profile: default
  cloud:
    config:
      uri: http://127.0.0.1:8888
      name: auth-service
    kubernetes:
      enabled: false
      config:
        enabled: false
      discovery:
        enabled: false